@model string
@using HourglassAzure.Models

<form asp-action="DeleteUser" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Deletar usuário" })

    <div class="modal-body form-horizontal">
       Deseja deletar o usuário <b>@Model</b>?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>

